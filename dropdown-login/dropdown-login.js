'use strict';

export default class DropdownLogin {
    constructor() {
        this.name = 'dropdown-login';
        // Activar botón de submit en el form de dropdown-login
        // var $inputs = $('.dropdown-login form input');
        // $inputs.on('keyup', function (e) {
        //   let notFilled = false;
        //   $inputs.each(function () {
        //     if (!$(this).val()) {
        //       notFilled = true;
        //       return false;
        //     }
        //   });
        //   $('.dropdown-login button[type="submit"]').prop('disabled', notFilled);
        // });
    }
}
