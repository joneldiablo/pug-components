'use strict';

import InputGroupIcon from '../input-group-icon';

describe('InputGroupIcon View', function() {

  beforeEach(() => {
    this.inputGroupIcon = new InputGroupIcon();
  });

  it('Should run a few assertions', () => {
    expect(this.inputGroupIcon).toBeDefined();
  });

});
