'use strict';

import StepsIndicatorH from '../steps-indicator';

describe('StepsIndicatorH View', function() {

  beforeEach(() => {
    this.stepsIndicatorH = new StepsIndicatorH();
  });

  it('Should run a few assertions', () => {
    expect(this.stepsIndicatorH).toBeDefined();
  });

});
