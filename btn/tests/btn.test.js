'use strict';

import Btn from '../btn';

describe('Btn View', function() {

  beforeEach(() => {
    this.btn = new Btn();
  });

  it('Should run a few assertions', () => {
    expect(this.btn);
  });

});
